{
  "name": "guitar-tuner-web",
  "version": "1.0.0",
  "description": "Afinador de guitarra en el navegador (Web Audio + autocorrelación). Port de la app iOS.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "icons": "node scripts/generate-icons.mjs",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^1.3.0"
  }
}
